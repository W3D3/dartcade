services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dartgames
      POSTGRES_USER: dartgames
      POSTGRES_PASSWORD: dev
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dartgames"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: sh -c "npm install --prefer-offline && npm run dev"
    environment:
      DATABASE_URL: postgres://dartgames:dev@postgres:5432/dartgames
      BRIDGE_SECRET: ${BRIDGE_SECRET:-devsecret}
      PORT: "3000"
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./backend/frontend:/app
      - /app/node_modules
    command: sh -c "npm install --prefer-offline && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
    depends_on:
      - backend

  bridge:
    build: ./bridge
    environment:
      DARTCADE_BOARD_URL: ${DARTCADE_BOARD_URL:?Set DARTCADE_BOARD_URL to your board's URL, e.g. http://192.168.1.x:3180}
      DARTCADE_BACKEND_URL: ws://backend:3000/bridge?secret=${BRIDGE_SECRET:-devsecret}
    depends_on:
      - backend
    restart: on-failure
